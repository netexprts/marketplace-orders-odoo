# Marketplace Orders → Odoo — ملاحظات المشروع

أداة HTML واحدة (`MP odoo orders.html`) بتحوّل تقارير الطلبات من نون وأمازون وترينديول لملف إكسل جاهز للاستيراد في أودو. الأداة فيها 4 تابات منفصلة، وكل واحد بيترفع لوحده لأن شكل تقرير المصدر مختلف لكل واحد.

## التابات التلاتة

### 1. NOON FBN (Fulfilled by Noon)
- **مصدر التقرير**: تصدير مبيعات نون العادي (أعمدة زي `item_nr, partner_sku, offer_price, status, fulfillment_model, bayan_nr, order_timestamp`...)
- **رقم الطلب**: من `item_nr` بعد شيل السفكس `-N` من الآخر
- **SKU**: من `partner_sku`
- **السعر**: من `offer_price` مباشرة (من غير خصم ضريبة)
- **تاريخ الطلب**: من `order_timestamp`
- **التجميع**: نفس الطلب + نفس الـ SKU = سطر واحد بالكمية مجمّعة
- **Warehouse**: `4003-Noon FBN` (ثابت)
- **Sales Team**: `NOON` | **carrier_id**: `NOON` | **Customer**: `6991326922357`

### 2. NOON FBP (Fulfilled by Partner)
- **مصدر التقرير**: تقرير `fbp_v2_dsv2_orderlisting` (أعمدة زي `order_nr, partner_sku, price, status, shipment_nr, received_at`...)
- **رقم الطلب**: من `order_nr` مباشرة (مفيش سفكس يتشال)
- **SKU**: من `partner_sku`
- **السعر**: من `price` مباشرة
- **تاريخ الطلب**: من `received_at`
- **التجميع**: نفس الطلب + نفس الـ SKU = سطر واحد؛ لو نفس المنتج اتكرر في أكتر من صف (تقسيم وحدات) بتتجمع الكمية
- **Warehouse**: `4001-مستودع العزيزيه` (ثابت)
- **Sales Team**: `NOON` | **carrier_id**: `NOON` | **Customer**: `6991326922357`
- **x_studio_shippingmethods**: دايمًا `FBP`

### 3. Amazon FBA/FBM
- **مصدر التقرير**: Amazon Order Listing (أعمدة زي `amazon-order-id, sku, item-price, quantity, order-status, fulfillment-channel, purchase-date`...)
- **رقم الطلب**: من `amazon-order-id` زي ما هو
- **SKU**: من `sku`
- **السعر**: `item-price ÷ quantity ÷ 1.15` (شيل ضريبة 15%)
- **تاريخ الطلب**: من `purchase-date`
- **التجميع**: نفس الطلب + نفس الـ SKU = سطر واحد بالكمية مجمّعة
- **الطلبات الملغية**: بيتم استبعادها (`order-status` = Cancelled/Canceled)
- **Warehouse (ديناميكي)**: حسب `fulfillment-channel` لكل طلب —
  - **Merchant** → `4001-مستودع العزيزيه`
  - **Amazon** (أو أي قيمة تانية) → `4002-Amazon FBA`
- **Sales Team**: `AMAZON` | **Customer**: `6991384765149` (مفيش carrier_id في تاب أمازون)

### 4. Trendyol
- **مصدر التقرير**: ملف Excel `Orders_...xlsx` (السطر الأول فيه بيان خصوصية/disclaimer، الهيدر الحقيقي في **السطر الثاني** — الأداة بتتعامل مع ده تلقائي عن طريق `headerRow:2`)
- **رقم الطلب**: من `رقم الطلبية` زي ما هو
- **SKU**: من `رمز المخزون`
- **السعر**: `مبلغ المبيعات ÷ العدد` (العمود ده إجمالي السطر مش سعر الوحدة)
- **تاريخ الطلب**: من `تاريخ الطلبية` بصيغة `DD.MM.YYYY` (مش زي باقي المنصات)
- **carrier_id (ديناميكي)**: ماخده من عمود `شركة الشحن` نفسه لكل طلب (مش قيمة ثابتة) — مثلاً J&T
- **Warehouse**: `4001-مستودع العزيزيه` (ثابت)
- **Sales Team**: `TRENDYOL` | **Customer**: `6991326922357` (نفس عميل نون)
- **x_studio_shippingmethods**: دايمًا `TRENDYOL`
- **حالة الطلب**: التقرير بيديها بالعربي مباشرة (تمّ التسليم / تم الشحن / منشأة / لم يتم التسليم) — "لم يتم التسليم" (محاولة توصيل فشلت) بتتحط **مرتجع**

## الأعمدة المشتركة الجديدة (بتتضاف آخر كل سطر، على مستوى الـ SKU مش الطلب)

| العمود | المصدر | ملاحظات |
|---|---|---|
| `x_studio_shipmenttrackingreference` | NOON FBN: `bayan_nr` — NOON FBP: `shipment_nr` — Amazon: فاضي (التقرير مفيهوش عمود تتبع) — Trendyol: `رقم تتبع الشحنة` | |
| `x_studio_shipmentstatus` | موحّد من حالة المصدر (`status` في نون، `order-status` في أمازون، `حالة الطلبية` بالعربي في ترينديول) | القيم النهائية: **تم التوصيل / قيد التنفيذ / ملغي / مرتجع / تالف** |
| `x_studio_shippingmethods` | NOON FBN: مستخرج من `fulfillment_model` (زي FBN)، غير كده بيفضل زي ما هو (زي Supermall) — NOON FBP: ثابت FBP — Amazon: Amazon→FBA / Merchant→FBM — Trendyol: ثابت TRENDYOL | |

### منطق توحيد حالة الشحن (unifyStatus)
بيدور على كلمات مفتاحية في نص الحالة الخام (إنجليزي أو عربي، بأي حروف كبيرة/صغيرة)، بالترتيب ده:
1. فيها نفي + كلمة تسليم (زي "لم يتم التسليم") → **مرتجع**
2. فيها "cancel"/"الغ"/"ملغ" → **ملغي**
3. فيها "return"/"ارجاع"/"مرتج" → **مرتجع**
4. فيها "damag"/"lost"/"unfulfillable"/"تالف"/"تلف" → **تالف**
5. فيها "deliver"/"تسليم"/"توصيل" → **تم التوصيل**
6. غير كده (Pending/Processing/Shipped/تم الشحن/منشأة...) → **قيد التنفيذ**

مثال: "Shipped - Returned to Seller" في أمازون، أو "لم يتم التسليم" في ترينديول، بتاخدوا **مرتجع** مش تم التوصيل، لأن فحص النفي/return قبل فحص deliver.

## هيكل السطور في الإكسل الناتج
- أول سطر لكل طلب بيحمل بيانات الطلب (Sales Team, Warehouse, Customer, Order Date)
- لو الطلب فيه أكتر من SKU، الأسطر اللي بعد كده بتسيب أعمدة الطلب فاضية (زي ما أودو محتاج)
- الأعمدة الستة الأخيرة (Product, Qty, Price, Tracking Ref, Shipment Status, Shipping Method) بتتملى في **كل سطر** لأنها بيانات على مستوى الـ SKU

## ملفات المشروع على الجهاز
```
Marketplace Orders - Odoo/
├── MP odoo orders.html          ← الأداة (افتحيها في المتصفح مباشرة)
├── PROJECT_NOTES.md             ← الملف ده
└── data/
    ├── noon_fbn_sample.csv      ← عينة تقرير NOON FBN
    ├── noon_fbp_sample.csv      ← عينة تقرير NOON FBP
    ├── amazon_sample.txt        ← عينة تقرير Amazon
    └── trendyol_sample.xlsx     ← عينة تقرير Trendyol
```

## قرارات اتاخدت أثناء البناء
- الحالات النهائية اتفقنا تبقى الـ5 كلهم (تم التوصيل / قيد التنفيذ / ملغي / مرتجع / تالف)، مش 4.
- قيمة "Supermall" في `fulfillment_model` بتاع NOON FBN بتتسيب زي ما هي (من غير تحويل لـ FBN/FBP).
- تاريخ الطلب في NOON FBP اتاخد من `received_at`.
- الـ Warehouse بتاع NOON FBP اتغيّر من "Noon fbp" لـ `4001-مستودع العزيزيه` بناءً على طلب لاحق.
- الـ Warehouse بتاع أمازون بقى ديناميكي (مش ثابت) حسب `fulfillment-channel`.
- تاب Trendyol جديد: الأداة بقت تدعم ملفات إكسل بيبدأ الهيدر الحقيقي فيها من سطر غير الأول (`headerRow`)، و`carrier_id` بقى ممكن ياخده الأداة ديناميكي من عمود في التقرير نفسه مش بس من قيمة ثابتة.
- "لم يتم التسليم" في ترينديول (محاولة توصيل فشلت) اتحطت **مرتجع**.

## الهوية البصرية (اللوجوهات الحقيقية)
- استُبدلت كل اللوجوهات المرسومة يدويًا (SVG تقريبية) بصور حقيقية اتبعتت من العميل: لوجو Noon الأصفر، ولوجو Trendyol البرتقالي، ولوجو الشركة الأساسي (اللوجو الكامل في الهيدر + الأيقونة).
- الصور اتقصت (crop) وشفافيتها اتظبطت (خلفية شفافة حوالين المحتوى بس مش الفراغات الداخلية) وتصغرت لحجم مناسب للويب، وتحطت جوه الملف نفسه كـ base64 (مفيش صور خارجية).
- شكل الهيدر اتغيّر من دايرة بإطار متقطع دوّار (logo-circle) لـ "لوحة" بيضا نضيفة بظل خفيف (logo-plate) حاطة فيها اللوجو الكامل.
- تابات Noon (الاتنين) وTrendyol بقت بتعرض اللوجو الحقيقي كصورة بدل الرمز SVG التقريبي. تاب أمازون سايبينه زي ما هو (رمز SVG) لحد ما ييجي لوجو حقيقي من العميل.

### تحديث: هوية الشركة اتغيّرت لـ NetX (Net Expert)
- لوجو Etaret Elhekma (اللي كان في الهيدر والفافيكون والفوتر) اتستبدل بالكامل بلوجو **NetX / Net Expert** الحقيقي اللي بعته العميل.
- العنوان (Title Tag) بقى "NetX — Marketplace Orders Odoo".
- الفافيكون بقى أيقونة حرف الـ X المميز من لوجو NetX (اتقص من اللوجو الكامل وشفافيته اتظبطت).
- اللوجو الكامل (NetX + Net Expert) اتحط في نفس "لوحة" الهيدر (logo-plate) بدل لوجو Etaret Elhekma.
- سطر الفوتر بقى: "Built with ♥ by NetX · Net Expert" بدل الإشارة لـ Etaret Elhekma.
- منصات نون وترينديول وأمازون في التابات لسه زي ما هي (مش جزء من هوية الشركة).

### تحديث: هوية الألوان (Color Identity) من ألوان اللوجو
- اتسحبت الألوان الأساسية من لوجو NetX نفسه: أزرق فاتح لامع (`#1068C0`) من تدرّج حرف الـ X، وكحلي غامق (`#0B2454`) من كتابة "NetX" السودا.
- كل الألوان القديمة اللي كانت جاية من هوية Etaret Elhekma (أخضر طبيعي `#7FB539` وبني ترابي `#5C3A28`) اتبدّلت بالكامل بالأزرق والكحلي دول — في كل مكان: العناوين، الأزرار، حدود التاب النشط، خلفيات الهيدر المتدرجة، ظلال الكروت، لون الخلفية العامة للصفحة (بقت درجة رمادي-أزرق فاتحة جدًا بدل الكريمي).
- ورق الشجر الزخرفي في الخلفية (leaves-bg) اتلوّن بنفس الأزرق/الكحلي الجديدين بدل الأخضر/البني القديمين، عشان يفضل شكل زخرفي خفيف من غير ما يرجّع لهوية Etaret.
- ألوان منصات نون (أصفر) وأمازون (برتقالي/كحلي) وترينديول (برتقالي) اتسابت زي ما هي تمامًا لأنها ألوان المنصات نفسها مش جزء من هوية الشركة.
- الألوان الدلالية (نجاح/تحذير/خطأ) اتسابت شبه زي ما هي، بس لون "نجاح" اتغيّر لأخضر عادي منفصل (`#2E9E44`) بعد ما الأخضر بقى مش لون الهوية.

### تحديث: أيقونة جديدة (N بالسهم) + شريط تنقل علوي
- الفافيكون (أيقونة التاب) اتبدّلت بلوجو NetX التاني اللي بعته العميل (حرف N بسهم متدرج أزرق/كحلي جوه مربع بحواف دائرية) بدل أيقونة حرف الـ X القديمة.
- اتضاف شريط تنقل علوي (`topnav`) بعرض الصفحة بالكامل، فوق الهيدر، بخلفية كحلي غامقة، يحاكي شكل الناف بار في موقع الشركة (test.netexprts.com):
  - على الشمال: أيقونة NetX الجديدة + اسم "NetX"
  - في النص: روابط تنقل ديكورية (Home / Solutions / Orders / Reports / Support) — مش وظيفية، بس شكلية زي الموقع
  - على اليمين: أيقونة بحث (عدسة مكبرة) جوه كبسولة، ديكورية برضه
- الشريط ده مستقل عن الـ container (بياخد عرض الصفحة كله) وبيختفي روابط التنقل والنص جنب أيقونة البحث في الشاشات الصغيرة (mobile) للحفاظ على شكل نضيف.
